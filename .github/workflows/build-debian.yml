name: Build Debian Package

on:
  push:
    branches: [ main, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

jobs:
  build-debian-x86_64:
    runs-on: ubuntu-latest
    container:
      image: debian:bullseye
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install system dependencies
      run: |
        apt-get update
        apt-get install -y curl build-essential pkg-config libssl-dev libfontconfig1-dev
        
    - name: Install Rust
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH
        
    - name: Add Rust target
      run: |
        source $HOME/.cargo/env
        rustup target add x86_64-unknown-linux-gnu
        
    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-debian-cargo-x86_64-unknown-linux-gnu-${{ hashFiles('**/Cargo.lock') }}
        
    - name: Build for Debian x86_64
      run: |
        source $HOME/.cargo/env
        cargo build --release --target x86_64-unknown-linux-gnu
        
    - name: Create package directory
      run: |
        mkdir -p package/nettest-debian-x86_64
        
    - name: Copy binary
      run: |
        cp target/x86_64-unknown-linux-gnu/release/nettest package/nettest-debian-x86_64/
        
    - name: Create README for package
      run: |
        cat > package/nettest-debian-x86_64/README.txt << 'EOF'
        Nettest - Network Speed Measurement Tool
        
        Architecture: x86_64
        Distribution: Debian Bullseye
        Build Date: $(date)
        
        Installation:
        1. Make binary executable: chmod +x nettest
        2. Run: ./nettest -s (server) or ./nettest -c <address> (client)
        
        For more information, visit: https://github.com/your-repo/nettest
        EOF
        
    - name: Create archive
      run: |
        cd package
        tar -czf nettest-debian-x86_64.tar.gz nettest-debian-x86_64/
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: nettest-debian-x86_64-latest
        path: package/nettest-debian-x86_64.tar.gz
        
    - name: Upload binary for release
      uses: actions/upload-artifact@v4
      with:
        name: nettest-debian-x86_64-binary
        path: target/x86_64-unknown-linux-gnu/release/nettest

  build-debian-aarch64:
    runs-on: ubuntu-latest
    container:
      image: debian:bullseye
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install system dependencies
      run: |
        apt-get update
        apt-get install -y curl build-essential pkg-config libssl-dev libfontconfig1-dev
        
    - name: Install Rust
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH
        
    - name: Add Rust target
      run: |
        source $HOME/.cargo/env
        rustup target add aarch64-unknown-linux-gnu
        
    - name: Install cross-compilation tools
      run: |
        apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
        
    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-debian-cargo-aarch64-unknown-linux-gnu-${{ hashFiles('**/Cargo.lock') }}
        
    - name: Build for Debian ARM64
      env:
        CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
        CXX_aarch64_unknown_linux_gnu: aarch64-linux-gnu-g++
        AR_aarch64_unknown_linux_gnu: aarch64-linux-gnu-ar
      run: |
        source $HOME/.cargo/env
        cargo build --release --target aarch64-unknown-linux-gnu
        
    - name: Create package directory
      run: |
        mkdir -p package/nettest-debian-aarch64
        
    - name: Copy binary
      run: |
        cp target/aarch64-unknown-linux-gnu/release/nettest package/nettest-debian-aarch64/
        
    - name: Create README for package
      run: |
        cat > package/nettest-debian-aarch64/README.txt << 'EOF'
        Nettest - Network Speed Measurement Tool
        
        Architecture: aarch64
        Distribution: Debian Bullseye
        Build Date: $(date)
        
        Installation:
        1. Make binary executable: chmod +x nettest
        2. Run: ./nettest -s (server) or ./nettest -c <address> (client)
        
        For more information, visit: https://github.com/your-repo/nettest
        EOF
        
    - name: Create archive
      run: |
        cd package
        tar -czf nettest-debian-aarch64.tar.gz nettest-debian-aarch64/
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: nettest-debian-aarch64-latest
        path: package/nettest-debian-aarch64.tar.gz
        
    - name: Upload binary for release
      uses: actions/upload-artifact@v4
      with:
        name: nettest-debian-aarch64-binary
        path: target/aarch64-unknown-linux-gnu/release/nettest

  create-debian-release:
    needs: [build-debian-x86_64, build-debian-aarch64]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Download binaries
      uses: actions/download-artifact@v4
      
    - name: List downloaded files
      run: |
        echo "Listing downloaded files:"
        find . -name "nettest" -type f
        ls -la nettest-debian-x86_64-binary/ || echo "Debian x86_64 binary not found"
        ls -la nettest-debian-aarch64-binary/ || echo "Debian aarch64 binary not found"
        
    - name: Create archives with binaries
      run: |
        # Create Debian x86_64 archive
        cp nettest-debian-x86_64-binary/nettest nettest
        chmod +x nettest
        tar -czf nettest-debian-x86_64.tar.gz nettest
        rm nettest
        
        # Create Debian ARM64 archive
        cp nettest-debian-aarch64-binary/nettest nettest
        chmod +x nettest
        tar -czf nettest-debian-aarch64.tar.gz nettest
        rm nettest
        
    - name: Create latest release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: latest-debian
        name: Nettest Latest Release (Debian)
        files: |
          nettest-debian-x86_64.tar.gz
          nettest-debian-aarch64.tar.gz
        token: ${{ secrets.GITHUB_TOKEN }}
        overwrite: true
        draft: false
        prerelease: false
        fail_on_unmatched_files: false 